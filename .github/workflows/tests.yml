# Run the test suite on every push and pull request.
#
# This does not block anything. It is evidence, not a gate: a green tick
# beside a commit saying the security regression tests reproduced, without
# anyone having to remember to run them. Making it required is a separate
# decision, and one that changes how releases work.
#
# Actions are pinned to commit SHAs rather than tags. A tag is a moving
# pointer; if it is ever repointed, a workflow that names it runs whatever it
# now points at. That matters more here than in most repositories.
name: tests

on:
  push:
  pull_request:

permissions:
  contents: read

jobs:
  suite:
    runs-on: ubuntu-latest
    steps:
      - name: Check out
        # actions/checkout v4.1.7
        uses: actions/checkout@692973e3d937129bcbf40652eb9f2f61becf3332

      - name: Python version
        run: python3 --version

      - name: Install promtool and amtool
        # Alert rules and Alertmanager routing are their own languages; the
        # tests that validate them skip without these, so CI has never run
        # them. A green badge over unrun checks is the same failure as the
        # Python file that shipped unparsed.
        #
        # Pinned and checksummed. "latest" would mean the audited SHA no
        # longer determines what the build used.
        run: |
          set -euo pipefail
          PROM_VER=3.5.0
          PROM_SHA=e811827af26d822afb09a4f28314f61b618b12cff5369835a67f674d8b46f39a
          AM_VER=0.28.1
          AM_SHA=5ac7ab5e4b8ee5ce4d8fb0988f9cb275efcc3f181b4b408179fafee121693311

          cd "$(mktemp -d)"
          curl -sSfLO "https://github.com/prometheus/prometheus/releases/download/v${PROM_VER}/prometheus-${PROM_VER}.linux-amd64.tar.gz"
          echo "${PROM_SHA}  prometheus-${PROM_VER}.linux-amd64.tar.gz" | sha256sum -c -
          tar xzf "prometheus-${PROM_VER}.linux-amd64.tar.gz"
          sudo install -m 755 "prometheus-${PROM_VER}.linux-amd64/promtool" /usr/local/bin/promtool

          curl -sSfLO "https://github.com/prometheus/alertmanager/releases/download/v${AM_VER}/alertmanager-${AM_VER}.linux-amd64.tar.gz"
          echo "${AM_SHA}  alertmanager-${AM_VER}.linux-amd64.tar.gz" | sha256sum -c -
          tar xzf "alertmanager-${AM_VER}.linux-amd64.tar.gz"
          sudo install -m 755 "alertmanager-${AM_VER}.linux-amd64/amtool" /usr/local/bin/amtool

          promtool --version
          amtool --version

      - name: No test may skip for a missing tool
        # The suite skips cleanly without these binaries, which is right on a
        # laptop and wrong here. If the install step above ever silently stops
        # working, this catches it rather than letting the badge go green over
        # checks nobody ran.
        run: |
          command -v promtool >/dev/null || { echo "promtool missing"; exit 1; }
          command -v amtool   >/dev/null || { echo "amtool missing";   exit 1; }

      - name: Server and agent tests
        # run.py already exits non-zero on failure, so no extra plumbing.
        # Some tests skip without promtool/amtool; that is expected here and
        # the run is still meaningful - the authorization and session tests
        # do not need them.
        run: python3 tests/run.py

      - name: Python files parse
        # tests/run.py only imports the test modules, so a .py that no test
        # imports is never compiled. monitor/xxops-linkspeed-peers.py shipped
        # with a syntax error and a green badge because of that gap - the
        # export had corrupted it and nothing read it as code.
        #
        # Discovered, not listed, for the same reason the shell check is.
        run: |
          python3 - <<'PYCHECK'
          import ast, os, sys
          bad = []
          for root, dirs, files in os.walk("."):
              dirs[:] = [d for d in dirs if d not in (".git", "__pycache__")]
              for f in files:
                  if not f.endswith(".py"):
                      continue
                  p = os.path.join(root, f)
                  try:
                      with open(p, encoding="utf-8") as fh:
                          ast.parse(fh.read())
                  except SyntaxError as e:
                      bad.append("%s:%s %s" % (p, e.lineno, e.msg))
                  except (OSError, UnicodeDecodeError) as e:
                      bad.append("%s unreadable: %s" % (p, e))
                  else:
                      print("ok  %s" % p)
          if bad:
              print("")
              print("these do not parse:")
              print("\n".join(bad))
              sys.exit(1)
          PYCHECK

      - name: Shell scripts parse
        # bash -n alone would NOT have caught the real bug here: a block of
        # shell landed inside die()'s printf string, which is malformed but
        # still valid shell. So also check that every function definition is
        # a single line that ends where it should.
        run: |
          # Discovered, not listed. The hand-written list covered five of
          # ten scripts and silently omitted the producer - the largest
          # shell file here, and where both root bugs lived.
          for f in $(find . -name '*.sh' -not -path './.git/*' | sort); do
            bash -n "$f" && echo "ok  $f"
          done
          python3 - <<'CHECK'
          import sys
          bad = []
          for f in ("install-monitor.sh", "agent/install.sh"):
              for n, line in enumerate(open(f), 1):
                  if line.startswith(("die()", "say()")) and not line.rstrip().endswith("}"):
                      bad.append("%s:%d %s" % (f, n, line.strip()[:60]))
          if bad:
              print("a one-line function is no longer one line:")
              print("\n".join(bad)); sys.exit(1)
          print("ok  helper functions are intact")
          CHECK

      - name: App JavaScript tests
        # node is present on ubuntu-latest. If it ever is not, this step
        # should fail loudly rather than pass quietly - a skipped test that
        # looks green is worse than a red one.
        run: |
          node --version
          node tests/app_tests.js
